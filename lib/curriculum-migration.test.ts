import {describe,expect,it} from 'vitest';import {readFileSync} from 'node:fs';import {resolve} from 'node:path';
const sql=readFileSync(resolve('supabase/migrations/006_curriculum_foundation.sql'),'utf8');
describe('curriculum migration contract',()=>{it('defines the normalized hierarchy and compatibility map',()=>{for(const table of ['curriculum_tracks','curriculum_modules','learning_objectives','objective_prerequisites','curriculum_lessons','lesson_variants','legacy_lesson_mappings'])expect(sql).toContain(`table public.${table}`)});it('enables RLS and published-only learner reads',()=>{expect(sql.match(/enable row level security/g)?.length).toBeGreaterThanOrEqual(9);expect(sql).toContain("state='published'");expect(sql).toContain('curriculum_has_role')});it('guards prerequisite cycles and reviewed publication',()=>{expect(sql).toContain('prevent_objective_cycle');expect(sql).toContain("current.state='reviewed'");expect(sql).toContain('reviewed_by is not null')})});
