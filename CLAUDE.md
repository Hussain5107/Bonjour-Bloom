# Claude Code instructions

Use `AGENTS.md` as the primary repository instruction file. Then read `README.md` and `docs/PROJECT_STATUS.md`.

Bonjour Bloom is an existing production French-learning PWA, not a greenfield prototype. Preserve working systems and Git history. Use a feature branch, make bounded changes, run the documented verification commands, and do not deploy without explicit user authorization.

The canonical architecture, development, and deployment references are linked from `AGENTS.md`.
